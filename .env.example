POSTGRES_USER=ethara
POSTGRES_PASSWORD=ethara
POSTGRES_DB=ethara
# Use `db` inside Docker Compose; use `localhost` for local uv runs
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Host port for the frontend container (maps to nginx :8080 inside)
FRONTEND_PORT=3000
